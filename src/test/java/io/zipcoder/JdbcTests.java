package io.zipcoder;
import static org.junit.Assert.*;
import io.zipcoder.persistenceapp.controllers.PersonController;
import io.zipcoder.persistenceapp.models.Person;
import io.zipcoder.persistenceapp.services.PersonService;
import org.junit.Test;
 public class JdbcTests {

    @Test
    public void testGetPerson(){
//        PersonService service = new PersonService();
//        PersonController controller = new PersonController(service);
//        Person person = new Person(1l,"K","R","88","302",12);
//        controller.persistPerson(person);
    }
}
